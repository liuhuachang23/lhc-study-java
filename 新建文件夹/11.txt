1、自定义枚举类 Enumeration01

    1）不需要通过set方法，因为枚举对象值通常为只读
    2）对枚举对象/属性使用 final static 共同修饰，实现底层优化
    3）枚举对象名通常使用全部大写（常量名的规范）
    4）枚举对象根据需要，也可以有多个属性

    自定义枚举实现步骤：
        （1）将构造器私有化
        （2）去掉set方法,防止属性被修改
        （3）在Season内部,直接创建对象
        （4）优化，可以加入 final 修饰，实现底层优化

2、使用enum关键字来实现枚举类 Enumeration02

    相比与 自定义枚举类 使用enum关键字 实现枚举类，更加简介

    实现步骤
        1）class替换为enum (枚举类)
        2）public static final Season2 SPRING = new Season2("春天","温暖") 用 SPRING("春天","温暖") 代替

    注意事项
        1）当我们使用enum关键字开发一个枚举类时，默认会继承 Enum类 （可以使用javap工具来演示）
            故使用 enum关键字后，就不能继承其他类了，但是可以实现接口
        2）传统的 public static final Season2 SPRING = new Season2（"春天"，“温暖");
            简化成 SPRING("春天”，"温暖”)，这里必须知道，它调用的是哪个构造器
        3）如果使用无参构造器 创建 枚举对象，则实参列表和小括号都可以省略
        4）当有多个枚举对象时，使用,间隔，最后有一个分号结尾
        5）枚举对象心须放在枚羊类的行首。

    枚举类的各种使用方法 EnumMethod