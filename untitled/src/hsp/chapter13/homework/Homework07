1.试分析 HashSet 和 TreeSet 如何实现去重机制

（1）、HashSet 的去重机制：hashCode() + equals(), 底层先通过存入对象，进行运算得到一个hash值，
  通过哈希值得到相应的索引，如果发现table索引所在的位置，没有数据就直接存放，如果有就进行equals
  的比较，相同就放弃比较，如果比较后不相同，然后在判断该链表结构是否为红黑树，是就进行红黑树机制处理
  不是就进入下一个循环，判断前面比较的元素后面有没有元素，没有就加入到后面，有就依次进行equals比较。
（2）、TreeSet 的去重机制：如果你传入了一个Comparator匿名对象，就使用实现的compare去重，如果返回0，就认
  为是相同元素/数据，就不添加，如果没有传入Comparator匿名对象，则以你添加的对象实现的Comparable接口
  的compareTo去重




